# ===============기존=================
# === Bridge Server (Python 3.10) ===
# threading 모드 사용 (eventlet 제거)
python-socketio[client]==5.11.3
python-engineio==4.12.3
werkzeug>=2.0.0  # WSGI 서버용 (threading 모드에서 필요)

# === Google Cloud STT ===
google-cloud-speech==2.20.0

# === Audio Processing ===
sounddevice==0.5.3
soundfile==0.12.1
numpy==1.26.4
scipy==1.11.4
tflite-runtime==2.14.0  # 이미 설치돼 있으면 생략 가능

# ===============추가=================
jamo